# {{{projectName}}}
{{#appDescription}}
{{{appDescription}}}
{{/appDescription}}

- API version: {{appVersion}}
- Package version: {{packageVersion}}
{{^hideGenerationTimestamp}}
- Build date: {{generatedDate}}
{{/hideGenerationTimestamp}}
- Build auth: yaLong

## Requirements.

Python 3.6+

## Installation & Usage
### pip install

```sh
pip install -r requirements.txt
```

If the python package is hosted on Github, you can install directly from Github

```sh
pip install git+https://github.com/{{{gitUserId}}}/{{{gitRepoId}}}.git
```
(you may need to run `pip` with root permission: `sudo pip install git+https://github.com/{{{gitUserId}}}/{{{gitRepoId}}}.git`)

Then import the package:
```python
import {{{packageName}}}
from {{{apiPackage}}}.{{{classVarName}}} import {{{classname}}}
from {{{apiPackage}}}.api_client import ApiClient
from configuration import Configuration, MarketplaceRegion

refresh_token = "Atzr|IRbFOMiGxxx"
role_arn = "arn:aws:iam::00xxxxx:role/SellirAllRolesxxx"
aws_access_key = 'xxx'
aws_secret_key = "xxx"
client_id = 'amzn1.application48eb1xxx'
client_secret = 'xxx'
marketplace_ids = [MarketplaceRegion.US.marketplace]
config = Configuration(role_arn=role_arn, refresh_token=refresh_token, aws_access_key=aws_access_key,
aws_secret_key=aws_secret_key, client_id=client_id, client_secret=client_secret, region = "US")
api_client = ApiClient(configuration=config)
api_instance = {{{classname}}}(api_client)

api_response = api_instance.xxx('112-941666-87519', marketplace_ids,_request_timeout=120)
print(api_response)

```

## Documentation for API Endpoints

All URIs are relative to *{{basePath}}*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
{{#apiInfo}}{{#apis}}{{#operations}}{{#operation}}*{{classname}}* | [**{{operationId}}**]({{apiDocPath}}{{classname}}.md#{{operationIdLowerCase}}) | **{{httpMethod}}** {{path}} | {{#summary}}{{summary}}{{/summary}}
{{/operation}}{{/operations}}{{/apis}}{{/apiInfo}}

## Documentation For Models

{{#models}}{{#model}} - [{{{classname}}}]({{modelDocPath}}{{{classname}}}.md)
{{/model}}{{/models}}

